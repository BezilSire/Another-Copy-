
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isAdmin() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    function isAgent() {
        return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'agent';
    }

    // --- PUBLIC & SYSTEM COLLECTIONS ---
    
    match /ledger/{docId} {
      allow read, list: if isSignedIn();
      allow create: if isSignedIn();
    }

    match /members/{memberId} {
      allow read, list: if isSignedIn();
      allow write: if isSignedIn(); 
    }

    match /p2p_offers/{offerId} {
      allow read, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn(); 
      allow delete: if isSignedIn() && resource.data.sellerId == request.auth.uid;
    }

    match /pending_ubt_purchases/{docId} {
      allow read, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isAdmin();
      allow delete: if isAdmin();
    }

    match /sell_requests/{docId} {
        allow read, list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isAdmin() || isAgent() || (isSignedIn() && resource.data.userId == request.auth.uid);
        allow delete: if isAdmin();
    }

    // --- USER PROFILES ---

    match /users/{userId} {
      allow read: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if isSignedIn();
      
      allow update: if isOwner(userId) 
                    || isAdmin()
                    || (isSignedIn() && request.resource.data.diff(resource.data).affectedKeys().hasOnly([
                        'followers', 'following', 'ubtBalance', 'publicKey', 'ccap', 'scap', 
                        'lastDailyCheckin', 'hasReadKnowledgeBase', 'currentCycleCcap', 
                        'lastCycleChoice', 'stakedCcap', 'ventureEquity', 'fcmToken', 'lastSeen'
                    ]));
      
      allow delete: if isAdmin();

      match /notifications/{notificationId} {
        allow read, write: if isOwner(userId) || isSignedIn();
      }

      match /transactions/{txId} {
        allow read: if isOwner(userId) || isAdmin();
        allow write: if isOwner(userId) || isAdmin() || isSignedIn(); 
      }
    }

    // --- CONTENT & SOCIAL ---

    match /posts/{postId} {
      allow read, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if resource.data.authorId == request.auth.uid 
                    || isAdmin() 
                    || (isSignedIn() && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['upvotes', 'commentCount', 'repostCount', 'isPinned']));
      allow delete: if resource.data.authorId == request.auth.uid || isAdmin();

      match /comments/{commentId} {
        allow read, list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if resource.data.authorId == request.auth.uid 
                      || isAdmin() 
                      || (isSignedIn() && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['upvotes']));
        allow delete: if resource.data.authorId == request.auth.uid || isAdmin();
      }
    }

    match /conversations/{conversationId} {
      allow read: if isSignedIn() && resource.data.members.hasAny([request.auth.uid]);
      allow list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn() && (resource.data.members.hasAny([request.auth.uid]) || isAdmin());

      match /messages/{messageId} {
        allow read, list, write: if isSignedIn(); 
      }
    }

    match /broadcasts/{broadcastId} {
      allow read, list: if isSignedIn();
      allow write: if isAdmin();
    }

    match /reports/{reportId} {
      allow read, list: if isAdmin();
      allow create: if isSignedIn();
      allow update: if isAdmin();
    }

    match /activity/{activityId} {
      allow read, list: if isSignedIn();
      allow write: if isSignedIn();
    }

    match /proposals/{proposalId} {
      allow read, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn(); 
      
      match /comments/{commentId} {
        allow read, list, write: if isSignedIn();
      }
    }

    match /ventures/{ventureId} {
      allow read, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if resource.data.ownerId == request.auth.uid || isAdmin() || isSignedIn();
      allow delete: if resource.data.ownerId == request.auth.uid || isAdmin();
      
      match /distributions/{distId} {
        allow read, list: if isSignedIn();
      }
    }

    match /payouts/{payoutId} {
      allow read, list: if isOwner(resource.data.userId) || isAdmin();
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
      allow update: if isAdmin();
    }

    match /globals/{docId} {
      allow read, list: if isSignedIn();
      allow update: if isSignedIn();
      allow write: if isAdmin();
    }

    match /sustenance_cycles/{docId} {
      allow read, list: if isSignedIn();
      allow write: if isAdmin();
    }

    match /sustenance_vouchers/{voucherId} {
      allow read, list: if isOwner(resource.data.userId) || isAdmin();
      allow update: if isAdmin() || isSignedIn(); 
    }
    
    match /price_verifications/{docId} {
        allow read, list: if isAdmin();
        allow create: if isSignedIn();
    }
  }
}
